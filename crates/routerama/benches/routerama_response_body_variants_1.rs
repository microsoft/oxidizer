// Copyright (c) Microsoft Corporation.
// Licensed under the MIT License.

// @generated by scripts/generate_response_body_variants.rs; DO NOT EDIT.
// Constant controls: 16 routes and 1 distinct handler body type.

#![allow(missing_docs, reason = "generated compile-size fixture needs no API documentation")]
#![expect(clippy::panic, reason = "a pending or failed in-memory fixture is a benchmark invariant violation")]

use std::convert::Infallible;
use std::pin::{Pin, pin};
use std::task::{Context, Poll, Waker};

use bytes::Bytes;
use http_body::{Body as HttpBody, Frame, SizeHint};
use routerama::response::{Body, Response};
use routerama::route::Request;

struct VariantBody<const ID: usize>(Body);

impl<const ID: usize> HttpBody for VariantBody<ID> {
    type Data = Bytes;
    type Error = Infallible;

    fn poll_frame(mut self: Pin<&mut Self>, cx: &mut Context<'_>) -> Poll<Option<Result<Frame<Self::Data>, Self::Error>>> {
        Pin::new(&mut self.0).poll_frame(cx)
    }

    fn is_end_stream(&self) -> bool {
        self.0.is_end_stream()
    }

    fn size_hint(&self) -> SizeHint {
        self.0.size_hint()
    }
}

struct VariantService;

#[allow(
    clippy::allow_attributes,
    unknown_lints,
    clippy::unused_async,
    clippy::unused_async_trait_impl,
    reason = "router handlers must be async; `clippy::unused_async_trait_impl` does not exist before clippy 0.1.98"
)]
#[routerama::route::router]
impl VariantService {
    #[route(GET, "/variants/route-00")]
    async fn route_00(&self) -> Response<VariantBody<0>> {
        Response::new(VariantBody(Body::from(Bytes::from_static(b"variant-route-00"))))
    }

    #[route(GET, "/variants/route-01")]
    async fn route_01(&self) -> Response<VariantBody<0>> {
        Response::new(VariantBody(Body::from(Bytes::from_static(b"variant-route-01"))))
    }

    #[route(GET, "/variants/route-02")]
    async fn route_02(&self) -> Response<VariantBody<0>> {
        Response::new(VariantBody(Body::from(Bytes::from_static(b"variant-route-02"))))
    }

    #[route(GET, "/variants/route-03")]
    async fn route_03(&self) -> Response<VariantBody<0>> {
        Response::new(VariantBody(Body::from(Bytes::from_static(b"variant-route-03"))))
    }

    #[route(GET, "/variants/route-04")]
    async fn route_04(&self) -> Response<VariantBody<0>> {
        Response::new(VariantBody(Body::from(Bytes::from_static(b"variant-route-04"))))
    }

    #[route(GET, "/variants/route-05")]
    async fn route_05(&self) -> Response<VariantBody<0>> {
        Response::new(VariantBody(Body::from(Bytes::from_static(b"variant-route-05"))))
    }

    #[route(GET, "/variants/route-06")]
    async fn route_06(&self) -> Response<VariantBody<0>> {
        Response::new(VariantBody(Body::from(Bytes::from_static(b"variant-route-06"))))
    }

    #[route(GET, "/variants/route-07")]
    async fn route_07(&self) -> Response<VariantBody<0>> {
        Response::new(VariantBody(Body::from(Bytes::from_static(b"variant-route-07"))))
    }

    #[route(GET, "/variants/route-08")]
    async fn route_08(&self) -> Response<VariantBody<0>> {
        Response::new(VariantBody(Body::from(Bytes::from_static(b"variant-route-08"))))
    }

    #[route(GET, "/variants/route-09")]
    async fn route_09(&self) -> Response<VariantBody<0>> {
        Response::new(VariantBody(Body::from(Bytes::from_static(b"variant-route-09"))))
    }

    #[route(GET, "/variants/route-10")]
    async fn route_10(&self) -> Response<VariantBody<0>> {
        Response::new(VariantBody(Body::from(Bytes::from_static(b"variant-route-10"))))
    }

    #[route(GET, "/variants/route-11")]
    async fn route_11(&self) -> Response<VariantBody<0>> {
        Response::new(VariantBody(Body::from(Bytes::from_static(b"variant-route-11"))))
    }

    #[route(GET, "/variants/route-12")]
    async fn route_12(&self) -> Response<VariantBody<0>> {
        Response::new(VariantBody(Body::from(Bytes::from_static(b"variant-route-12"))))
    }

    #[route(GET, "/variants/route-13")]
    async fn route_13(&self) -> Response<VariantBody<0>> {
        Response::new(VariantBody(Body::from(Bytes::from_static(b"variant-route-13"))))
    }

    #[route(GET, "/variants/route-14")]
    async fn route_14(&self) -> Response<VariantBody<0>> {
        Response::new(VariantBody(Body::from(Bytes::from_static(b"variant-route-14"))))
    }

    #[route(GET, "/variants/route-15")]
    async fn route_15(&self) -> Response<VariantBody<0>> {
        Response::new(VariantBody(Body::from(Bytes::from_static(b"variant-route-15"))))
    }
}

static SERVICE: VariantService = VariantService;

fn run_ready<F: std::future::Future>(future: F) -> F::Output {
    let mut future = pin!(future);
    let mut context = Context::from_waker(Waker::noop());
    match future.as_mut().poll(&mut context) {
        Poll::Ready(output) => output,
        Poll::Pending => panic!("the generated route fixture must be immediately ready"),
    }
}

fn observe<B>(body: B) -> (usize, u64)
where
    B: HttpBody<Data = Bytes>,
    B::Error: std::fmt::Debug,
{
    let mut body = pin!(body);
    let mut context = Context::from_waker(Waker::noop());
    let mut length = 0;
    let mut hash = 0xcbf2_9ce4_8422_2325_u64;
    loop {
        match body.as_mut().poll_frame(&mut context) {
            Poll::Ready(Some(Ok(frame))) => {
                if let Ok(data) = frame.into_data() {
                    length += data.len();
                    for byte in data {
                        hash ^= u64::from(byte);
                        hash = hash.wrapping_mul(0x0000_0100_0000_01b3);
                    }
                }
            }
            Poll::Ready(Some(Err(error))) => panic!("fixture body failed: {error:?}"),
            Poll::Ready(None) => return (length, hash),
            Poll::Pending => panic!("the generated body fixture must be immediately ready"),
        }
    }
}

fn main() {
    let path = std::hint::black_box("/variants/route-15");
    let mut request = Request::new(());
    *request.uri_mut() = path.parse().expect("the generated fixture path is a valid URI");
    let response = run_ready(SERVICE.route(request, &()));
    let status = response.status().as_u16();
    std::hint::black_box((status, observe(response.into_body())));
}
