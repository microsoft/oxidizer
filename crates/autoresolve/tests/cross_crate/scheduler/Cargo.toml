[package]
name = "xc_scheduler"
version = "0.0.0"
edition.workspace = true
publish = false

[dependencies]
autoresolve = { workspace = true, features = ["macros"] }
xc_io_driver = { path = "../io_driver" }
