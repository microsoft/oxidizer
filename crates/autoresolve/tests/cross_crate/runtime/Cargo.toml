[package]
name = "xc_runtime"
version = "0.0.0"
edition.workspace = true
publish = false

[dependencies]
autoresolve = { workspace = true, features = ["macros"] }
xc_scheduler = { path = "../scheduler" }
