[[config.allow]]
rule = "m365_unsafe"
file = "Cargo.toml"
reason = "This workspace contains audited unsafe implementations in low-level crates. Forbidding unsafe at the virtual root would break unrelated existing crates; the workspace instead enforces unsafe_op_in_unsafe_fn and undocumented_unsafe_blocks."

[[config.allow]]
rule = "m365_mandated_libs"
file = "Cargo.toml"
reason = "native-tls is an existing supported TLS backend across the workspace. Replacing it would remove a public compatibility option and requires a separately approved repository-wide migration."

[[config.allow]]
rule = "m365_mandated_libs"
file = "crates/fetch/Cargo.toml"
reason = "fetch intentionally exposes native-tls as one of two supported TLS backends. Removing it is a breaking transport and feature change outside this PR."

[[config.allow]]
rule = "m365_tower"
file = "Cargo.toml"
reason = "The root dependency supports existing ecosystem integrations elsewhere in the workspace. Removing it requires a repository-wide consumer migration outside this PR."

[[config.allow]]
rule = "m365_layered"
file = "Cargo.toml"
reason = "tower-layer remains an existing compatibility dependency for workspace consumers while layered is the preferred middleware API. Removing it is a separate repository-wide migration."

[[config.allow]]
rule = "m365_layered"
file = "crates/fetch/Cargo.toml"
reason = "hyper-util HttpConnector implements tower-service::Service, so fetch needs the trait to drive that external transport boundary; fetch middleware itself is built with layered."

[[config.allow]]
rule = "m_crates_in_workspace"
file = "Cargo.toml"
reason = "Private test, example, benchmark, and automation members are intentionally omitted from workspace.dependencies. This repository requires dev-dependencies to use explicit sibling paths."

[[config.allow]]
rule = "m_crates_in_workspace"
file = "crates/compressors/Cargo.toml"
reason = "Repository convention requires explicit path entries for dev-dependencies so tests bind to the sibling source crate rather than a workspace dependency declaration."

[[config.allow]]
rule = "m_crates_in_workspace"
file = "crates/fetch/Cargo.toml"
reason = "Repository convention requires explicit path entries for dev-dependencies so tests bind to the sibling source crate rather than a workspace dependency declaration."

[[config.allow]]
rule = "m_crates_in_workspace"
file = "crates/http_compression/Cargo.toml"
reason = "Repository convention requires explicit path entries for dev-dependencies so tests bind to the sibling source crate rather than a workspace dependency declaration."

[[config.allow]]
rule = "m_errors_canonical_structs"
file = "crates/compressors/src/error.rs"
reason = "The existing canonical error structs already retain kind, message, source, and recovery data. Capturing a Backtrace in every compression error would add material cost and alter an established public error representation."

[[config.allow]]
rule = "m_dont_leak_types"
file = "crates/compressors/src/error.rs"
reason = "recoverable is a workspace sibling that defines the shared retry classification contract used across Oxidizer. The direct Recovery implementation is deliberate cross-crate interoperability."

[[config.allow]]
rule = "prefer_ohno_over_std_error"
file = "crates/compressors/src/error.rs"
reason = "The established compressors errors provide custom kind accessors, owned source extraction, and recoverable classification. Replacing them with an ohno-derived public error is a breaking redesign outside this PR."

[[config.allow]]
rule = "m_essential_fn_inherent"
file = "crates/compressors/src/error.rs"
reason = "Recovery is a shared workspace trait intentionally implemented across error types; adding duplicate inherent recovery methods would create two paths for the same contract."

[[config.allow]]
rule = "c_custom_type"
file = "crates/compressors/src/stream.rs"
reason = "flush_on_pending follows the established builder toggle convention and is part of the ported public API. Replacing bool with a new public enum is an API commitment that requires explicit breaking-change approval."

[[config.allow]]
rule = "c_getter"
file = "crates/compressors/src/stream.rs"
reason = "get_ref, get_mut, get_pin_mut, and into_inner intentionally mirror standard adapter APIs such as BufReader and pin-project wrappers. The suggested name ref is also a Rust keyword."

[[config.allow]]
rule = "m365_templated_uri_direct"
file = "crates/fetch/src/telemetry.rs"
reason = "server_port consumes http::Request::uri(), whose ecosystem type is http::Uri. Converting it to templated_uri would parse or allocate on the telemetry path and lose the raw request boundary."

[[config.allow]]
rule = "c_struct_private"
file = "crates/fetch/src/custom.rs"
reason = "CustomDeps and CustomContext are deliberate passive records constructed and destructured by transport authors. They protect no invariant, matching the guideline's documented data-carrier exception."

[[config.allow]]
rule = "m_parameter_consistency"
file = "crates/fetch/src/custom.rs"
reason = "Reordering create_builder parameters would break the existing public custom-transport API. No behavior or safety defect justifies that breaking change in this PR."

[[config.allow]]
rule = "m_weasel_words"
file = "crates/fetch/src/custom.rs"
reason = "The report explicitly found no violating type name in this file; factory is only a parameter describing an Fn value, which the guideline permits."

[[config.allow]]
rule = "c_custom_type"
file = "crates/fetch/src/handlers/metrics.rs"
reason = "report_total_duration is an existing public builder toggle. Replacing bool with a new public enum is a breaking API change outside this PR."

[[config.allow]]
rule = "m_dont_leak_types"
file = "crates/fetch/src/handlers/metrics.rs"
reason = "OpenTelemetry interoperability is a primary fetch feature, and these existing APIs intentionally accept its Meter, MeterProvider, and KeyValue types. Wrapping or gating them would break consumers."

[[config.allow]]
rule = "m_dont_leak_types"
file = "crates/fetch/src/client_builder.rs"
reason = "OpenTelemetry provider injection is an established public fetch capability. Gating or wrapping MeterProvider would break existing telemetry consumers and is outside this feature PR."

[[config.allow]]
rule = "m_foreign_reexports"
file = "crates/fetch/src/lib.rs"
reason = "fetch is intentionally a facade over its HTTP, resilience, and templated-URI sibling contracts so consumers can use one coherent client API. Removing established re-exports is breaking."

[[config.allow]]
rule = "m_log_not_print"
file = "crates/fetch/src/lib.rs"
reason = "The reported println calls occur only in doctest examples to display returned values; production fetch code uses structured telemetry and does not log through stdout."

[[config.allow]]
rule = "m_single_item_path"
file = "crates/fetch/src/lib.rs"
reason = "The duplicate paths are deliberate foreign re-exports from sibling crates in the fetch facade, which is the exception described by the foreign-reexports guideline."

[[config.allow]]
rule = "m_build_result"
file = "crates/fetch/src/options/decompression.rs"
reason = "ResponseDecompressionOptions is the final immutable options value, not a builder for another type. Adding build() or changing setters to NonZeroU64 would break the newly ported public API."

[[config.allow]]
rule = "m_canonical_docs"
file = "crates/fetch/src/options/decompression.rs"
reason = "The report confirms every panicking method has a Panics section and identifies no concrete missing contract; requiring an example on every trivial constructor is not a correctness gap."

[[config.allow]]
rule = "m_dont_leak_types"
file = "crates/fetch/src/options/mod.rs"
reason = "fetch_options and http_extensions are workspace siblings split for transport reuse; fetch intentionally re-exports their shared option types as its facade API."

[[config.allow]]
rule = "m_collection_traits"
file = "crates/fetch/src/telemetry.rs"
reason = "TelemetryAttributes intentionally exposes a narrow push-and-slice API. Adding public iterator types and IntoIterator implementations is a new public commitment not required by this feature."

[[config.allow]]
rule = "m_dont_leak_types"
file = "crates/fetch/src/telemetry.rs"
reason = "OpenTelemetry KeyValue is the established telemetry interchange type, and ConnectionInfo comes from the fetch_options sibling crate. Wrapping either would break existing consumers."

[[config.allow]]
rule = "c_custom_type"
file = "crates/http_compression/src/compression.rs"
reason = "advertise is a public builder toggle in the ported API. Replacing bool with a new public enum requires explicit API-change approval."

[[config.allow]]
rule = "m_dont_leak_types"
file = "crates/http_compression/src/compression.rs"
reason = "http_compression is the HTTP adapter for the compressors sibling crate, so Format, Level, Limits, and Resources are intentionally its shared interoperability contract."

[[config.allow]]
rule = "m_async_fn"
file = "crates/http_compression/src/compression.rs"
reason = "The explicit impl Future uses Either to keep the middleware future materially smaller than an async state machine. The user explicitly requires preserving this size-sensitive pattern."

[[config.allow]]
rule = "m_services_clone"
file = "crates/http_compression/src/compression.rs"
reason = "CompressionLayer is Clone and is the supported way to construct additional services. Compression wraps an arbitrary RequestHandler, whose contract does not require Clone, so adding a public conditional Clone impl would create an unnecessary API commitment."

[[config.allow]]
rule = "m_avoid_indirection"
file = "crates/http_extensions/src/body/builder.rs"
reason = "OpaqueMemory is required only for caller-supplied MemoryShared implementations; the common GlobalPool path remains concrete. Removing type erasure would remove custom-memory support."

[[config.allow]]
rule = "m_async_fn"
file = "crates/http_extensions/src/body/mod.rs"
reason = "These established public methods return explicit Send futures to preserve runtime portability and control future composition. Converting them changes public future types and requires separate API review."

[[config.allow]]
rule = "m_integration_tests"
file = "crates/compressors/src/error.rs"
reason = "The inline tests exercise crate-private Kind and constructors. M-INTEGRATION-TESTS applies only to tests that touch public API surface."

[[config.allow]]
rule = "m_integration_tests"
file = "crates/compressors/src/pool.rs"
reason = "The inline tests inspect private engine maps, capacity, and poison state. Moving them would require exposing implementation details, outside M-INTEGRATION-TESTS scope."

[[config.allow]]
rule = "m_integration_tests"
file = "crates/compressors/src/stream.rs"
reason = "The inline suite drives private pump behavior and crate-private test codecs in addition to public methods. It is a unit-test module, not a public-API integration suite."

[[config.allow]]
rule = "m_integration_tests"
file = "crates/fetch/src/client.rs"
reason = "The inline tests inspect private pipeline state and error-label plumbing, so they require crate-private access and remain unit tests."

[[config.allow]]
rule = "m_single_item_path"
file = "crates/fetch/src/client.rs"
reason = "The client module is private and HttpClient has one public path through the crate-root re-export."

[[config.allow]]
rule = "m_integration_tests"
file = "crates/fetch/src/client_builder.rs"
reason = "The inline tests inspect private builder options and pipeline representation, which cannot be tested through public API alone."

[[config.allow]]
rule = "m_single_item_path"
file = "crates/fetch/src/client_builder.rs"
reason = "The client_builder module is private and HttpClientBuilder has one public path through the crate-root re-export."

[[config.allow]]
rule = "m_integration_tests"
file = "crates/fetch/src/custom.rs"
reason = "The inline tests exercise the crate-private transport factory and dependency plumbing; moving them would require exposing internals."

[[config.allow]]
rule = "m_integration_tests"
file = "crates/fetch/src/error_labels.rs"
reason = "The inline tests target private label-resolution helpers and constants, so they are unit tests outside the guideline's public-API qualifier."

[[config.allow]]
rule = "m_integration_tests"
file = "crates/fetch/src/handlers/dispatch.rs"
reason = "The inline tests construct private DispatchMode values and validate private routing logic, requiring crate-private access."

[[config.allow]]
rule = "m_essential_fn_inherent"
file = "crates/fetch/src/handlers/dispatch.rs"
reason = "Dispatch is a middleware endpoint whose execution contract is layered::Service; it is not directly constructed or called by consumers, and duplicating execute would add a second internal path."

[[config.allow]]
rule = "m_integration_tests"
file = "crates/fetch/src/handlers/metrics.rs"
reason = "The inline tests inspect private callbacks, drop guards, and attribute helpers; they are implementation unit tests."

[[config.allow]]
rule = "m_integration_tests"
file = "crates/fetch/src/options/decompression.rs"
reason = "The inline tests verify private format mapping and limits conversion in addition to public option methods, requiring crate-private access."

[[config.allow]]
rule = "m_integration_tests"
file = "crates/fetch/src/options/mod.rs"
reason = "The inline test inspects the crate-private ClientOptions aggregate and cannot be expressed through public API alone."

[[config.allow]]
rule = "m_design_for_ai"
file = "crates/fetch/src/options/mod.rs"
reason = "The report incorrectly states the test module is empty; client_options_default_uses_https_filter verifies the private aggregate default."

[[config.allow]]
rule = "m_integration_tests"
file = "crates/fetch/src/pipeline/custom.rs"
reason = "The inline test covers the crate-private custom-pipeline callable wrapper and its debug representation."

[[config.allow]]
rule = "m_avoid_wrappers"
file = "crates/fetch/src/pipeline/custom.rs"
reason = "CustomPipeline is crate-private and keeps performables::Arc entirely inside its representation; its create method exposes only the dynamic service, so the public-wrapper guideline does not apply."

[[config.allow]]
rule = "m_design_for_ai"
file = "crates/fetch/src/pipeline/custom.rs"
reason = "The report incorrectly states the tests module is empty. The module contains debug_custom_pipeline, which constructs CustomPipeline and verifies its observable Debug representation; pipeline execution is covered by pipeline::builder tests."

[[config.allow]]
rule = "m_integration_tests"
file = "crates/fetch/src/pipeline/builder.rs"
reason = "The inline tests construct crate-private pipeline variants and inspect private debug and standard-pipeline markers, so they require unit-test placement."

[[config.allow]]
rule = "m_async_fn"
file = "crates/fetch/src/pipeline/builder.rs"
reason = "The inherent execute method deliberately returns an explicit Send future to mirror the Service trait path and avoid adding another async state machine on the request hot path."

[[config.allow]]
rule = "m_panic_message"
file = "crates/fetch/src/pipeline/builder.rs"
reason = "The panic message states both the expected custom pipeline and the actual minimal pipeline variant; the report's claim that the observed variant is absent is factually incorrect."

[[config.allow]]
rule = "m_services_clone"
file = "crates/fetch/src/pipeline/builder.rs"
reason = "Pipeline owns a type-erased DynamicService and a Dispatch, neither of which is Clone-bound. HttpClient shares Pipeline behind performables::Arc, so cloning the service value itself is unnecessary."

[[config.allow]]
rule = "m_integration_tests"
file = "crates/fetch/src/pipeline/standard.rs"
reason = "The inline suite inspects private layer fields and construction helpers, so it is intentionally colocated unit coverage."

[[config.allow]]
rule = "m_integration_tests"
file = "crates/fetch/src/telemetry.rs"
reason = "The inline tests exercise private scope builders and protocol attribute helpers, requiring crate-private access."

[[config.allow]]
rule = "m_integration_tests"
file = "crates/http_compression/src/negotiate.rs"
reason = "The negotiation parser and quality type are private implementation details; their exhaustive table tests require private access."

[[config.allow]]
rule = "m_integration_tests"
file = "crates/http_extensions/src/body/builder.rs"
reason = "The inline tests inspect private body policy state and memory dispatch, so they are implementation unit tests."

[[config.allow]]
rule = "m_integration_tests"
file = "crates/http_extensions/src/body/mod.rs"
reason = "The inline tests exercise private body variants, consumption state, and buffering helpers, requiring crate-private access."

[[config.allow]]
rule = "m_integration_tests"
file = "crates/http_extensions/src/body/options.rs"
reason = "The inline tests verify the crate-private merge policy as well as public setters, so they require unit-test placement."

[[config.allow]]
rule = "m_init_cascaded"
file = "crates/fetch/src/client.rs"
reason = "HttpClient::new is a single-call-site private field constructor with four distinct fields. Introducing another carrier type would add indirection without preventing an observed bug."

[[config.allow]]
rule = "m_weasel_words"
file = "crates/fetch/src/client.rs"
reason = "The report explicitly identifies no Service, Manager, Factory, Helper, or Util type declared in this file; HttpClientPipeline names the domain value it stores."

[[config.allow]]
rule = "m_single_item_path"
file = "crates/fetch/src/pipeline/custom.rs"
reason = "PipelineConstructor is private and CustomPipeline is crate-private with one re-export path. The report found no duplicate public item path."

[[config.allow]]
rule = "m_weasel_words"
file = "crates/fetch/src/pipeline/custom.rs"
reason = "Two remediation attempts renamed the private dyn Fn alias from Factory to Call to Constructor, and the rule flagged each synonym. The alias is private and names a callable signature; further renaming would be terminology oscillation rather than a design fix."

[[config.allow]]
rule = "m_short_names"
file = "crates/fetch/src/pipeline/standard.rs"
reason = "StandardRequestPipeline is an established public type whose full name distinguishes it from custom and minimal pipelines. Renaming it is a breaking API change."
